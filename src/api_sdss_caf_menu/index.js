const getMenu = require('./controller/getMenu.js');

/*
* Collection of the different functions of the caf-menu api.
* They are called by the router:
*/

const cafMenuApi = {
  getMenu
};

module.exports = cafMenuApi;
